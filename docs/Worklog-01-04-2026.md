# Worklog 01-04-2026

## Tasks completed

- Created backend-first v2 scaffold under `src_v2`.
- Implemented auth/session policy and role-permission guard.
- Implemented initial backend slices for patients, appointments, and billing.
- Added SQLite schema/repositories and container bootstrap.
- Added v1-to-v2 migration script and release/migration architecture docs.

## Problems encountered

- Existing repo has tight coupling between UI and data logic, requiring explicit service boundaries in v2.
- Mixed naming between v1 entities (`clients`, `reservations`) and v2 entities (`patients`, `appointments`) required mapping rules.

## Decisions made

- Prioritized backend contracts first, postponing UI modernization until contracts stabilize.
- Selected SQLite as first infrastructure adapter for deterministic local testing.
- Added default admin bootstrap for predictable authentication in fresh v2 databases.

## Next steps

- Expand backend slices to secondary modules.
- Add Supabase adapter parity for repository ports.
- Add higher-coverage tests and execute migration dry-runs with real snapshots.

## Additional progress

- Added secondary operations backend service for doctors, rooms, and equipment.
- Extended v2 SQLite schema and repositories for operations entities.
- Added automated test coverage for operations resource creation.
- Added clinical and insurance backend services for prescriptions, clinical notes, and claims.
- Extended v2 SQLite schema/repositories for `prescriptions`, `clinical_notes`, and `insurance_claims`.
- Added tests covering clinical/insurance creation flow.
- Verified and fixed migration/reporting defects:
  - explicit semantic field mapping from v1 to v2 for appointments/invoices
  - accurate migrated row counting based on real insert deltas, not loop iterations
  - proper DB connection cleanup in migration flow
- Corrected permission namespaces:
  - clinical operations now require `clinical:write`
  - operations-domain writes now require `operations:write`
- Added regression tests for migration mapping/counting and permission namespace behavior.
- Re-verified and restored bugfixes after regression:
  - invoice amount migration keeps numeric typing via explicit float conversion
  - migrated row counts now track real inserts (ignores not counted)
  - clinical APIs enforce `clinical:write`
  - operations APIs enforce `operations:write`
